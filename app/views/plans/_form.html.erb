<div class="container" style="margin-top:60px">
<div class="shadow">
<div>
<div class="form-horizontal">
<%= form_for (@plans) do |f| %>
  <% if @plans.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plans.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @plans.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


                            <div class="control-group">
							<label class="control-label" for="form-field-2"><%= f.label :name %></label>

									<div class="controls">
									  <label>
										<%= f.text_field :name %>
										</label>
									</div>
								</div>
								
								
						
					
						
								
  
 <div class="form-actions">
    <%= f.submit "Submit",:class=>"btn btn-primary" %>
    
  <span class="extra_buttons">
  	
  	<%= link_to 'Back', plans_path %>


</span>
</div>
<% end %>
</div>								
</div>
</div>
</div>